name: Build & Publish main

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: carbonrunner-2vcpu-ubuntu-latest
    steps:
      - name: Say hello
        run: echo "hello"
      - name: Sleeptime
        run: sleep 45
